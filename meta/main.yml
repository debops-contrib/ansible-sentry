---
dependencies:
  - role: debops.secret

# dependencies:

#        - role: debops.redis
#          # We want the sentinel on the same server as redis
#          redis_sentinel_standalone: False
#
#          # We dont need it, runs only on localhost
#          redis_requirepass: False
#          redis_slave_read_only: 'no'
#
#          redis_sentinel_bind: ['0.0.0.0']
#          redis_sentinel_allow: ['0.0.0.0']
#          tags: [ 'depend:redis' ]

#        - role: debops.postgresql_server
#          postgresql_server__hba_system:
#            - comment: 'Openup for all local connections ipv6'
#              type: 'local'
#              database: 'all'
#              user: 'all'
#              address: [ '::1/128' ]
#              method: 'md5'
#
#            - comment: 'Openup for all local connections'
#              type: 'local'
#              database: 'all'
#              user: 'all'
#              address: [ '127.0.0.1/32' ]
#              method: 'md5'
#          tags: [ 'postgresql_server' ]
